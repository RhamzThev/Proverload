import Realm from "realm";

export class Circuit {
    static schema = {
        name: "Circuit",
        properties: {
            _id: "objectId",
            workoutId: "objectId",
        },
        primaryKey: '_id',
    }

    // CREATE

    // READ

    // UPDATE

    // DELETE

}